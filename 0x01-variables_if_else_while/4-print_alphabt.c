#include <stdio.h>
/**
 *main - this is the main function
 *
 *Return: always 0
 */
int main(void)
{
	char l;

	for (l = 'a' ; l <= 'z' ; l++)
		if (i != 'q' && i != 'e')
			putchar(l);
	putchar("\n");
	return (0);
}
